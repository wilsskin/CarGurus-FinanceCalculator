
import React from 'react';
import { useFinance } from '@/context/finance';

const EditButton = () => {
  const { state } = useFinance();

  return null; // Completely remove the button
};

export default EditButton;
